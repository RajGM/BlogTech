import AuthCheck from '../../components/AuthCheck';

export default function AdminPostsPage({ }) {
    return (<main>
        <h1>Admin Page</h1>
        <AuthCheck>

        </AuthCheck>
    </main>)
}